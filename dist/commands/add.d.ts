export interface AddOptions {
    copyEnv?: boolean;
}
/**
 * Add a worktree for an existing branch (local or remote)
 */
export declare function add(branch: string, options?: AddOptions): Promise<void>;
//# sourceMappingURL=add.d.ts.map